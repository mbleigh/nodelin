#!/usr/local/Cellar/node/0.1.104/bin/node
// generated by npm, please don't touch!
var depMet = require.paths.indexOf("./dependencies") !== -1
if (!depMet) require.paths.unshift("./dependencies")
module.exports = require("./package/main")
if (!depMet) {
  var i = require.paths.indexOf("./dependencies")
  if (i !== -1) require.paths.splice(i, 1)
}